const extglob = require('extglob')

module.exports.isExtglob = (pattern) => {
  if (pattern === 'hex' || pattern === 'emoji') return true
  try {
    extglob(pattern)
    return true
  } catch (error) {
    return false
  }
}
